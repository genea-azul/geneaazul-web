# SPA catch-all — all other paths (unknown routes, story slugs, etc.)
# Route-specific HTML entry points live at root (e.g. buscar.html) so Cloudflare's
# native clean-URL serving handles /buscar → buscar.html transparently (no redirect).
# Keep in sync with: scripts/generate-route-pages.js ROUTES, _headers SPA route entries.
/* /index.html 200

# Genea Azul — Website

Public-facing website for [Genea Azul](https://geneaazul.com.ar), a non-profit community genealogy research project focused on the city of Azul (Buenos Aires, Argentina).

## Architecture

- **Static SPA** — single `index.html` shell with History API routing (`pushState`/`popstate`) and lazy-loaded page fragments
- **No build step** — plain HTML, CSS, and JavaScript (jQuery + Bootstrap 5)
- **Backend API** — calls `https://gedcom-analyzer-app.fly.dev` (Spring Boot on Fly.io)
- **Hosting** — Cloudflare Pages (auto-deploys from this repo)

## Quick Start

```bash
git clone https://github.com/genea-azul/geneaazul-web.git
cd geneaazul-web
```

### With full mock API (recommended)

`dev-server.js` serves static files **and** mocks all backend API endpoints so everything works without a running backend:

```bash
node dev-server.js
# → http://localhost:8090
```

All API endpoints are mocked with realistic data. Ephemerides and birthday entries are generated for today's Argentine date so landing-page features are visible immediately.

### Static files only

If you only need to browse static pages (no API-dependent features):

```bash
python3 -m http.server 8080
```

API calls will hit `gedcom-analyzer-app.fly.dev` directly (requires internet access). To point at a local backend instead, change `apiBaseUrl` in `js/config.js`.

## Project Structure

```plaintext
geneaazul-web/
├── index.html              # App shell: navbar, hero, footer; also the landing page
├── buscar.html             # Per-route SEO entry points (copies of index.html with
├── estadisticas.html       # route-specific <head> tags). Generated by scripts/
├── estadisticas/           # generate-route-pages.js — re-run after any <head> change
│   └── inmigracion.html    # in index.html. Cloudflare serves these at clean URLs.
├── …
├── css/
│   ├── main.css            # Shared layout and components
│   ├── theme-heritage.css  # Warm/earthy theme (browns, serif fonts)
│   └── theme-modern.css    # Clean/crisp theme (blues, sans-serif)
├── js/
│   ├── config.js           # API URL, feature flags, hero counters
│   ├── router.js           # History API routing + lazy loading
│   ├── birthdays.js        # #cumpleanos landing section (alive azuleños born today)
│   ├── ephemerides.js      # #efemerides landing section (distinguished persons this month)
│   ├── search.js           # Person search
│   ├── connections.js      # Relationship finder
│   ├── stats.js            # Statistics pages (immigration, personalities, surnames)
│   ├── map.js              # Interactive immigration map (Leaflet)
│   ├── cronologia.js       # Historical timeline page
│   ├── stories.js          # Markdown story rendering
│   ├── i18n.js             # Spanish localisation helpers
│   └── utils.js            # Shared utilities (apiGet, escHtml, etc.)
├── pages/                  # HTML fragments (lazy-loaded by router into #page-content)
│   ├── buscar.html
│   ├── conexiones.html
│   ├── estadisticas.html
│   ├── estadisticas-inmigracion.html
│   ├── estadisticas-personalidades.html
│   ├── estadisticas-apellidos.html
│   ├── mapa.html
│   ├── cronologia.html
│   ├── historias.html
│   ├── recursos.html
│   ├── testimonios.html
│   ├── colabora.html
│   └── sobre-nosotros.html
├── stories/                # Markdown files for family stories
│   ├── index.json          # Story metadata
│   └── *.md                # Individual stories
├── data/                   # Static JSON data
│   ├── immigration.json    # Immigration waves by origin country
│   ├── personalities.json  # 250+ notable persons
│   ├── surnames.json       # Surname list with frequency
│   └── timeline.json       # Historical timeline entries for /cronologia
├── img/                    # Images and SVG assets
├── scripts/
│   └── generate-route-pages.js  # Regenerates per-route HTML entry points from index.html
├── _redirects              # Cloudflare Pages SPA catch-all (/* → index.html 200)
├── _headers                # Cloudflare Pages cache headers per path
├── dev-server.js           # Local dev server with full API mocks (node dev-server.js)
├── docs/
│   └── API-REFERENCE.md    # Backend API documentation
└── README.md               # This file
```

## Documentation

- **[API Reference](docs/API-REFERENCE.md)** — full backend API documentation with request/response examples, DTO specs, error handling, date formats, and relationship type references

## Deployment

### Cloudflare Pages

1. Push to the `main` branch of this repo
2. Cloudflare Pages auto-deploys (no build step)
3. Custom domain `geneaazul.com.ar` is configured in Cloudflare DNS

### Manual

Upload all files to any static hosting provider. The `_redirects` file handles SPA routing on Cloudflare Pages; for other hosts, configure equivalent rewrite rules.

## CDN Dependencies

| Library         | Version | Purpose                                |
| --------------- | ------- | -------------------------------------- |
| Bootstrap       | 5.3.8   | Layout and components                  |
| Bootstrap Icons | 1.13.1  | Icons                                  |
| jQuery          | 3.7.1   | DOM manipulation, AJAX                 |
| Leaflet         | 1.9.4   | Interactive map                        |
| marked.js       | 15.0.12 | Markdown rendering                     |
| Google Fonts    | —       | Playfair Display, Source Sans 3, Inter |

## Contributing

This is a community project. To contribute family data, request tree access, or volunteer, contact:

- Instagram: [@genea.azul](https://instagram.com/genea.azul)
- Facebook: [genea.azul](https://facebook.com/genea.azul)
- Email: <genea.azul@gmail.com>
